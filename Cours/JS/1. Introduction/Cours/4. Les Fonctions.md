# <span style="color:gold;">Les Fonctions en JavaScript</span> 📜

---

## <span style="color:gold;">Introduction aux Fonctions</span> 🛠️

Les **fonctions** sont au cœur de la programmation en JavaScript.  
Elles permettent de **regrouper un ensemble d’instructions** pour effectuer une tâche spécifique.  
Une fonction peut être **appelée plusieurs fois**, ce qui rend votre code plus **modulaire, lisible et réutilisable**.

💡 **Tip Apprenant :**  
> Une fonction, c’est comme une “recette” : vous la définissez une fois, puis vous pouvez l’utiliser autant de fois que nécessaire, avec des ingrédients (paramètres) différents.

---

## <span style="color:gold;">Déclaration d’une Fonction</span> ✏️

La syntaxe de base pour définir une fonction est :

```js
function nomDeLaFonction(parametre1, parametre2, ...) {
    // Instructions
    return resultat;
}
```

- `function` : mot-clé qui indique que vous créez une fonction.  
- `nomDeLaFonction` : le nom que vous lui donnez (utilisez le camelCase).  
- `parametres` : les valeurs d’entrée que la fonction peut recevoir.  
- `return` : indique la valeur renvoyée par la fonction (facultatif).

### Exemple :
```js
function direBonjour() {
    console.log("Bonjour !");
}

// Appel de la fonction
direBonjour();
```

🧠 **Explication :**  
La fonction `direBonjour()` affiche “Bonjour !” à chaque fois qu’elle est appelée.

---

## <span style="color:gold;">Paramètres et Arguments</span> 📌

Les **paramètres** sont les variables déclarées dans la définition de la fonction,  
et les **arguments** sont les valeurs passées lors de l’appel.

### Exemple :
```js
function additionner(a, b) {
    return a + b;
}

console.log(additionner(5, 3)); // Affiche 8
console.log(additionner(10, 2)); // Affiche 12
```

💡 **Tip Apprenant :**
> Vous pouvez utiliser le mot-clé `return` pour renvoyer une valeur à l’endroit où la fonction a été appelée.  
> Si vous oubliez `return`, la fonction renverra `undefined`.

---

## <span style="color:gold;">Fonctions avec Valeur de Retour</span> 🔁

Une fonction peut retourner **un résultat** que vous pouvez stocker ou réutiliser.

```js
function carre(nombre) {
    return nombre * nombre;
}

let resultat = carre(4);
console.log(resultat); // Affiche 16
```

💡 **Tip Apprenant :**
> Le mot-clé `return` interrompt immédiatement l’exécution d’une fonction et renvoie la valeur donnée.

---

## <span style="color:gold;">Variables Locales et Portée</span> 🧩

Les variables définies **à l’intérieur d’une fonction** sont **locales** à celle-ci : elles ne sont pas accessibles depuis l’extérieur.

```js
function testPortee() {
    let interne = "Je suis à l'intérieur";
    console.log(interne); // OK
}

testPortee();
// console.log(interne); // ❌ Erreur : "interne is not defined"
```

💡 **Tip Apprenant :**
> Les fonctions permettent d’éviter les **conflits de noms** de variables grâce à cette portée locale.

---

## <span style="color:gold;">Fonctions Anonymes et Expressions Fonctionnelles</span> 🎭

Une **fonction anonyme** est une fonction sans nom.  
Elles sont souvent assignées à une variable.

### Exemple :
```js
const multiplier = function(x, y) {
    return x * y;
};

console.log(multiplier(4, 3)); // Affiche 12
```

💡 **Tip Apprenant :**
> Les fonctions anonymes sont utiles lorsque vous voulez stocker ou passer une fonction comme **valeur** (ex. : à un événement).

---

## <span style="color:gold;">Fonctions Fléchées (Arrow Functions)</span> ⚡

Introduites avec **ES6**, les fonctions fléchées offrent une **syntaxe plus concise**.

### Exemple classique :
```js
function soustraire(a, b) {
    return a - b;
}
```

### Version fléchée :
```js
const soustraire = (a, b) => a - b;

console.log(soustraire(10, 4)); // Affiche 6
```

💡 **Tips Apprenant :**
> Si votre fonction contient **une seule instruction**, vous pouvez **omettre les accolades `{}`** et le mot-clé `return`.

---

## <span style="color:gold;">Paramètres par Défaut</span> 🧮

Vous pouvez définir une **valeur par défaut** pour un paramètre.

```js
function saluer(nom = "visiteur") {
    console.log(`Bonjour, ${nom} !`);
}

saluer(); // Bonjour, visiteur !
saluer("Alice"); // Bonjour, Alice !
```

💡 **Tip Apprenant :**
> Cela évite d’avoir des erreurs si un argument est manquant.

---

## <span style="color:gold;">Fonctions avec Conditions et Boucles</span> 🔄

Les fonctions peuvent contenir des **structures de contrôle**, comme des conditions et des boucles.

```js
function afficherPairs(max) {
    for (let i = 0; i <= max; i++) {
        if (i % 2 === 0) {
            console.log(`${i} est pair`);
        }
    }
}

afficherPairs(10);
```

💡 **Tip Apprenant :**
> Combinez conditions et boucles dans vos fonctions pour automatiser des tâches répétitives.

---

## <span style="color:gold;">Fonctions Imbriquées</span> 🧠

Une fonction peut être **définie à l’intérieur d’une autre**.

```js
function exterieur() {
    function interieur() {
        console.log("Je suis à l'intérieur !");
    }
    interieur();
}

exterieur();
```

💡 **Tip Apprenant :**
> Cela permet de **créer des fonctions utilitaires internes** qui ne sont pas accessibles de l’extérieur.

---

## <span style="color:gold;">Les Fonctions comme Arguments</span> 🪄

En JavaScript, les fonctions sont des **valeurs de première classe**, c’est-à-dire qu’on peut les passer **en paramètre** à d’autres fonctions.

```js
function executerFonction(fonction, valeur) {
    return fonction(valeur);
}

function doubler(x) {
    return x * 2;
}

console.log(executerFonction(doubler, 5)); // Affiche 10
```

💡 **Tip Apprenant :**
> C’est la base de la **programmation fonctionnelle** et du comportement des **callbacks**.

---

## <span style="color:gold;">Fonctions Fléchées et Callbacks</span> 🔁

Les **callbacks** sont des fonctions passées en argument à d’autres fonctions.  
Elles sont souvent utilisées dans les événements ou les temporisations.

```js
setTimeout(() => {
    console.log("Message affiché après 2 secondes !");
}, 2000);
```

💡 **Tip Apprenant :**
> `setTimeout()` exécute une fonction après un certain temps (ici, 2000 millisecondes = 2 secondes).

---

## <span style="color:gold;">Résumé des Types de Fonctions</span> 🧾

| Type de fonction | Syntaxe | Exemple |
|------------------|----------|----------|
| **Déclaration** | `function nom() {}` | `function saluer() {}` |
| **Expression** | `const f = function() {}` | `const direBonjour = function() {}` |
| **Fléchée** | `const f = () => {}` | `const addition = (a, b) => a + b;` |
| **Callback** | `setTimeout(() => {}, 1000)` | Appel différé ou conditionnel |

---

## <span style="color:gold;">Conclusion</span> 🎉

Les **fonctions** sont le cœur du JavaScript moderne.  
Elles permettent de :
- Structurer le code  
- Réutiliser des morceaux logiques  
- Simplifier la maintenance  
- Créer des comportements dynamiques  

> 🚀 **Astuce finale :**  
> Entraînez-vous à écrire vos propres fonctions.  
> Expérimentez avec les paramètres, les valeurs de retour et les fonctions fléchées.  
> Plus vous en écrirez, plus vous comprendrez leur puissance !
